/**
 * Common Constants
 * Shared constants used across the extension
 */

// Schemes for special pages where the extension should be disabled
const BLOCKED_SCHEMES = [
  'chrome://',
  'chrome-extension://',
  'edge://',
  'about:'
];
